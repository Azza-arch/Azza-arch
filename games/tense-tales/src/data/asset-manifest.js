// Central list of assets. Scenes read from here instead of hardcoding
// paths, so real art can replace placeholders without touching gameplay code.

const AssetManifest = {
  images: [
    // { key: 'card-frame', path: 'assets/ui/card-frame.png' },
  ],
  spritesheets: [
    // { key: 'character-idle', path: 'assets/characters/idle.png', frameConfig: { frameWidth: 128, frameHeight: 128 } },
  ],
  audio: [
    // { key: 'click', path: 'assets/audio/click.mp3' },
  ],
};
